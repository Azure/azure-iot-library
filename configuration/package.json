{
  "name": "@azure-iot/configuration",
  "version": "1.0.0-rc.6",
  "description": "Configuration support for Azure IoT microservices",
  "main": "./dist/configuration.js",
  "typings": "./dist/configuration.d.ts",
  "engines": {
      "node": ">=6.1.0"
  },
  "author": "",
  "dependencies": {
    "mongodb": "^2.1.18"
  },
  "devDependencies": {
    "rimraf": "^2.5.2",
    "tsd": "^0.6.5",
    "tslint": "^3.9.0",
    "typescript": "^1.8.0",
    "gulp": "^3.9.1",
    "gulp-istanbul": "^1.0.0",
    "gulp-jasmine": "^2.3.0",
    "gulp-typescript": "^2.13.6",
    "remap-istanbul": "^0.6.4",
    "opener": "^1.4.1"
  },
  "scripts": {
    "lint": "tslint *.ts",
    "build": "tsc",
    "prebuild": "npm run lint",
    "prepublish": "rm -rf dist/* && tsd install && npm run build",
    "test": "npm run build && gulp test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Azure/azure-iot-library.git"
  },
  "license": "SEE LICENSE IN ./license.txt",
  "directories": {
    "lib": "./src"
  }
}
